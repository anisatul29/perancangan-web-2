<?php 
include "koneksi.php";

$query = mysqli_query($koneksi, "SELECT * FROM karyawan"); 
?>

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Karyawan</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #eef2f3;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 80%;
            margin: 50px auto;
            background: #ffffff;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 5px 12px rgba(0,0,0,0.1);
        }

        h2 {
            text-align: center;
            color: #003366;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        table th {
            background-color: #003366;
            color: #ffffff;
            padding: 12px;
            font-size: 14px;
        }

        table td {
            padding: 10px;
            text-align: center;
            background-color: #f7f9fb;
            border-bottom: 1px solid #d9d9d9;
        }

        .btn {
            padding: 8px 12px;
            border-radius: 6px;
            font-size: 13px;
            text-decoration: none;
            color: white;
        }

        .btn-edit { background-color: #007bff; }
        .btn-delete { background-color: #dc3545; }
        .btn-home { 
            background-color: #17a2b8; 
            margin-bottom: 15px; 
            display: inline-block;
        }

        .btn:hover {
            opacity: 0.8;
        }

        .no-data {
            text-align: center;
            padding: 20px;
            color: #777;
        }

    </style>
</head>
<body>

<div class="container">
    
    <h2>📌 DATA KARYAWAN LAUNDRY ONLINE</h2>

    <a href="karyawan.php" class="btn btn-home">⬅ Kembali ke Menu Karyawan</a>

    <table>
        <tr>
            <th>ID</th>
            <th>Nama Karyawan</th>
            <th>Jabatan</th>
            <th>No Telepon</th>
            <th>Aksi</th>
        </tr>

        <?php 
        if (mysqli_num_rows($query) > 0) {
            while ($data = mysqli_fetch_array($query)) { ?>
                
                <tr>
                    <td><?= $data['id_karyawan']; ?></td>
                    <td><?= $data['nama_karyawan']; ?></td>
                    <td><?= $data['jabatan']; ?></td>
                    <td><?= $data['no_telp']; ?></td>
                    <td>
                        <a href="edit_karyawan.php?id=<?= $data['id_karyawan']; ?>" class="btn btn-edit">✏ Edit</a>
                        <a href="hapus_karyawan.php?id=<?= $data['id_karyawan']; ?>" class="btn btn-delete" onclick="return confirm('Yakin ingin menghapus data ini?')">🗑 Hapus</a>
                    </td>
                </tr>

        <?php } 
        } else { ?>
            <tr>
                <td colspan="5" class="no-data">⚠ Belum ada data karyawan.</td>
            </tr>
        <?php } ?>
        
    </table>

</div>

</body>
</html>
